version: '3.8'
services:
  neo-dining:
    build: .
    image: neo-dining:latest
    container_name: neo_dining_app
    ports:
      - "5000:5000"
    volumes:
      # 挂载数据目录，实现数据持久化
      - ./backend/data:/app/data
    restart: always